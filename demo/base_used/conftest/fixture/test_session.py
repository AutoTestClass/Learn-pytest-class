def test_session_scope_1(session_scope_fixture):
    assert session_scope_fixture == "session scope fixture"


def test_session_scope_2(session_scope_fixture):
    assert session_scope_fixture == "session scope fixture"
